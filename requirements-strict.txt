# Requirements với version cố định (khuyến nghị cho production)
# Dùng file này nếu muốn đảm bảo tính nhất quán 100%

colorama==0.4.6
joblib==1.5.2
lightgbm==4.5.0
numpy==1.26.4
pandas==2.2.2
psutil==5.9.8
python-dateutil==2.9.0.post0
pytz==2024.1
scikit-learn==1.5.2
scipy==1.13.1
six==1.17.0
threadpoolctl==3.5.0
tqdm==4.66.4
tzdata==2024.1

# LIEF - Nếu dùng Python 3.8/3.9, dùng version này
# lief==0.9.0  # Chỉ dùng với Python 3.8/3.9

# LIEF - Nếu dùng Python 3.10+, dùng version này (có warning nhưng hoạt động)
lief==0.12.3

